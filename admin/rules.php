<?php require __DIR__.'/_common.php'; $actions=$guard->config()->actions(); ?>
<!doctype html><meta charset="utf-8"><title>Guard rules</title><style>body{font:14px system-ui;max-width:1100px;margin:32px auto}pre{background:#f5f6f8;padding:16px;overflow:auto}</style><h1>Effective action rules</h1><p>Read-only by design in 0.4.x. Change reviewed policy in protected server config, not through a public writable admin endpoint.</p><?php foreach($actions as $name=>$rule):?><h2><?=gh($name)?></h2><pre><?=gh(json_encode($rule,JSON_PRETTY_PRINT|JSON_UNESCAPED_UNICODE|JSON_UNESCAPED_SLASHES))?></pre><?php endforeach?>
